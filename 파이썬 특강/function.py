# python 함수 정의
# def 키워드 활용
# 중괄호 x, 들여쓰기로 코드 블럭을 구분한다.
# 콜론(:) 함수 정의부를 나타낸다.
def func(parameter):
    print(f"함수 실행, {parameter}")
    return parameter + 1
  
  
  # # 함수 호출
  # return_value = func(10) # 함수 실행, 10
  # print(return_value) # 11