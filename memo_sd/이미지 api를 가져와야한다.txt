이미지 api를 가져와야한다

가져온 api 정보를 변수로 선언한다.

img 태그를 이용해서 이미지를 불러온다.
img태그의 src속성에 절대경로를 사용한다.
절대경로를 사용하는 이유는 

url을 이용해서 불러오면 하나만 되기때문에 
전체를 불러오기위한 코드를 쓴다.



절대경로
컴퓨터에 있는 물리적 경로

상대경로
이미지를 삽입할 HTML 문서를 기준으로 경로를 인식하는 방법
